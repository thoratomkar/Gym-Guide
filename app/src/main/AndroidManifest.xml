<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.suraj.gymguid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme1">
            >
        </activity>
        <activity android:name=".MuscleGain">

        </activity>
        <activity android:name=".TestWorkout">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity
            android:name=".DietOnBasisOfBodyMass"
            android:screenOrientation="portrait" />
        <activity
            android:name=".FruitDisplay"
            android:theme="@style/AppTheme1" />
        <activity
            android:name=".VegetableDisplay"
            android:theme="@style/AppTheme1" />
        <activity
            android:name=".DairyDisplay"
            android:theme="@style/AppTheme1" />
        <activity
            android:name=".ProtienDisplay"
            android:theme="@style/AppTheme1" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WeightLoss"></activity>
    </application>

</manifest>